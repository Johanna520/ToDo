@page
@model ToDo.Pages.MyTodos2Model

@{
    ViewData["Title"] = "MyTodos2";
}

<h1>MyTodos2</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WhatTodo[0].Start)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WhatTodo[0].Task)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WhatTodo[0].Deadline)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WhatTodo[0].Done)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.WhatTodo) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Start)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Task)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deadline)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Done)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
